<Window x:Class="MSCC.Views.AiSearchPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AI Search" 
        Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        MinHeight="300"
        MinWidth="400"
        Icon="/app-icon.ico">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock x:Name="TitleText" Text="AI Search" 
                       FontSize="18" FontWeight="SemiBold" 
                       Foreground="#2C3E50"/>
            <TextBlock x:Name="DescriptionText" 
                       Text="Enter a system prompt to analyze the search results with AI."
                       Foreground="Gray" 
                       TextWrapping="Wrap"
                       Margin="0,4,0,0"/>
        </StackPanel>
        
        <!-- System Prompt -->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock x:Name="SystemPromptLabel" Grid.Row="0" 
                       Text="System Prompt:" 
                       FontWeight="Medium"
                       Margin="0,0,0,8"/>
            
            <TextBox x:Name="SystemPromptTextBox" Grid.Row="1"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     Padding="8"
                     Text="You are a helpful assistant. Analyze the following search results and provide a concise summary highlighting the most relevant information."/>
        </Grid>
        
        <!-- Results count info -->
        <TextBlock x:Name="ResultsInfoText" Grid.Row="2" 
                   Text="0 results will be analyzed"
                   Foreground="Gray"
                   FontSize="12"
                   Margin="0,12,0,0"/>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="CancelBtn" Content="Cancel" 
                    Padding="20,10" 
                    Margin="0,0,8,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="AnalyzeBtn" Content="Analyze with AI" 
                    Padding="20,10"
                    Background="#9B59B6"
                    Foreground="White"
                    Click="AnalyzeButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
