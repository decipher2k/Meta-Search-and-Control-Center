<UserControl x:Class="MSCC.Views.ResultDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="500">
    
    <Border Background="White" BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="12">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock x:Name="TitleBlock" 
                           FontSize="16" 
                           FontWeight="SemiBold"
                           TextWrapping="Wrap"/>
                <TextBlock x:Name="SourceBlock" 
                           FontSize="12" 
                           Foreground="Gray"
                           Margin="0,4,0,0"/>
            </StackPanel>
            
            <TextBlock Grid.Row="1" 
                       x:Name="DescriptionBlock"
                       TextWrapping="Wrap"
                       Margin="0,0,0,12"/>
            
            <ContentControl Grid.Row="2" 
                            x:Name="ContentArea"
                            Margin="0,0,0,12"/>
            
            <StackPanel Grid.Row="3" x:Name="ActionsPanel" Orientation="Horizontal"/>
        </Grid>
    </Border>
</UserControl>
