<Window x:Class="MSCC.Views.DataSourceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Datenquelle bearbeiten" 
        Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Name:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="NameTextBox" Padding="8,6"/>
        </StackPanel>
        
        <!-- Konnektor-Typ (nur bei Neuanlage) -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12" x:Name="ConnectorPanel">
            <TextBlock Text="Konnektor-Typ:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ComboBox x:Name="ConnectorComboBox" 
                      Padding="8,6"
                      DisplayMemberPath="Name"
                      SelectionChanged="ConnectorComboBox_SelectionChanged"/>
        </StackPanel>
        
        <!-- Gruppe -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Gruppe:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ComboBox x:Name="GroupComboBox" 
                      Padding="8,6">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="12" Height="12" Fill="{Binding Color}" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        
        <!-- Aktiviert -->
        <CheckBox Grid.Row="3" x:Name="IsEnabledCheckBox" Content="Aktiviert" IsChecked="True" Margin="0,0,0,12"/>
        
        <!-- Konfiguration -->
        <StackPanel Grid.Row="4" Margin="0,0,0,12">
            <TextBlock Text="Konfiguration:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="150">
                <ItemsControl x:Name="ConfigItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding DisplayName}" FontWeight="Medium"/>
                                    <TextBlock Text=" *" Foreground="Red" 
                                               Visibility="{Binding IsRequired, Converter={StaticResource BoolToVis}}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Description}" FontSize="11" Foreground="Gray" Margin="0,2,0,4"/>
                                <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                         Padding="8,6"
                                         Tag="{Binding Name}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Abbrechen" 
                    Padding="16,8" 
                    Margin="0,0,8,0"
                    Click="CancelButton_Click"/>
            <Button Content="Speichern" 
                    Padding="16,8"
                    Background="#27AE60"
                    Foreground="White"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
