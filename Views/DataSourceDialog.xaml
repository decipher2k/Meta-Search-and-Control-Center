<Window x:Class="MSCC.Views.DataSourceDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Data Source" 
        Height="450" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="/app-icon.ico">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="NameLabel" Text="Name:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="NameTextBox" Padding="8,6"/>
        </StackPanel>
        
        <!-- Konnektor-Typ (nur bei Neuanlage) -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12" x:Name="ConnectorPanel">
            <TextBlock x:Name="ConnectorLabel" Text="Connector Type:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ComboBox x:Name="ConnectorComboBox" 
                      Padding="8,6"
                      DisplayMemberPath="Name"
                      SelectionChanged="ConnectorComboBox_SelectionChanged"/>
        </StackPanel>
        
        <!-- Gruppe -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock x:Name="GroupLabel" Text="Group:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ComboBox x:Name="GroupComboBox" Padding="8,6">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="12" Height="12" Fill="{Binding Color}" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding Name}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
        
        <!-- Aktiviert -->
        <CheckBox Grid.Row="3" x:Name="IsEnabledCheckBox" Content="Enabled" IsChecked="True" Margin="0,0,0,12"/>
        
        <!-- Konfiguration -->
        <StackPanel Grid.Row="4" Margin="0,0,0,12">
            <TextBlock x:Name="ConfigLabel" Text="Configuration:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="150">
                <ItemsControl x:Name="ConfigItemsControl">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,8">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding DisplayName}" FontWeight="Medium"/>
                                    <TextBlock Text=" *" Foreground="Red" 
                                               Visibility="{Binding IsRequired, Converter={StaticResource BoolToVis}}"/>
                                </StackPanel>
                                <TextBlock Text="{Binding Description}" FontSize="11" Foreground="Gray" Margin="0,2,0,4"/>
                                <TextBox Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                         Padding="8,6"
                                         Tag="{Binding Name}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="CancelButton" Content="Cancel" 
                    Padding="16,8" 
                    Margin="0,0,8,0"
                    Click="CancelButton_Click"/>
            <Button x:Name="SaveButton" Content="Save" 
                    Padding="16,8"
                    Background="#27AE60"
                    Foreground="White"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
