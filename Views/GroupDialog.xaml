<Window x:Class="MSCC.Views.GroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gruppe bearbeiten" 
        Height="300" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Name -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Name:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="NameTextBox" Padding="8,6"/>
        </StackPanel>
        
        <!-- Beschreibung -->
        <StackPanel Grid.Row="1" Margin="0,0,0,12">
            <TextBlock Text="Beschreibung:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <TextBox x:Name="DescriptionTextBox" Padding="8,6" TextWrapping="Wrap" AcceptsReturn="True" Height="60"/>
        </StackPanel>
        
        <!-- Farbe -->
        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Farbe:" FontWeight="SemiBold" Margin="0,0,0,4"/>
            <StackPanel Orientation="Horizontal">
                <ComboBox x:Name="ColorComboBox" Width="200" Padding="8,6">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="16" Height="16" Margin="0,0,8,0">
                                    <Ellipse.Fill>
                                        <SolidColorBrush Color="{Binding Color}"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                                <TextBlock Text="{Binding Name}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <Ellipse x:Name="ColorPreview" Width="24" Height="24" Margin="12,0,0,0"/>
            </StackPanel>
        </StackPanel>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Abbrechen" 
                    Padding="16,8" 
                    Margin="0,0,8,0"
                    Click="CancelButton_Click"/>
            <Button Content="Speichern" 
                    Padding="16,8"
                    Background="#27AE60"
                    Foreground="White"
                    Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
